#!/bin/bash

# Launch notification daemon with colors

. "$HOME/.local/bin/getcolors"

kill -TERM "$(pidof dunst)"
dunst -lb "$BACKGROUND" -lf "$FOREGROUND" -lfr "$GREEN" \
	-nb "$BACKGROUND" -nf "$FOREGROUND" -nfr "$BLUE" \
	-cb "$BACKGROUND" -cf "$FOREGROUND" -cfr "$RED"
