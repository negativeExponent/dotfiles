#!/bin/bash

# Launch notification daemon with colors

#. "$HOME/.local/bin/getcolors"

#FG="$BACKGROUND"
#BG="$FOREGROUND"

kill -TERM "$(pidof dunst)"
#dunst -lb "$BG" -lf "$FG" -lfr "$GREEN" \
#	-nb "$BG" -nf "$FG" -nfr "$BLUE" \
#	-cb "$BG" -cf "$FG" -cfr "$RED"

dunst
